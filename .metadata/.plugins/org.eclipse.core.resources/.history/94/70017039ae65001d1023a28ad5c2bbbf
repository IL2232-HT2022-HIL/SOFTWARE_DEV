#include "setup.h"
#include "cmsis_os.h"

int Init_MsgQueue (osMessageQueueId_t Q_name, struct Q_struct) {

  Q_name = osMessageQueueNew(Q_struct, sizeof(Q_struct), NULL);
  if (Q_name == NULL) {
	  return -1;
  }
  return 0;
}

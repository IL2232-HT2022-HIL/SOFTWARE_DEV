

int gateway_receive_BUF = [10];
int pos = 0;

int* HIL_gateway_get_message(){
	MSGQ_obj = msg;
	status = osMessageQueueGet(UART_Q, &msg, NULL, 0U);
	if (status == osOK){
		gateway_receive_BUF[pos] = msg;
		pos = (pos +1)%10;
	}
}
